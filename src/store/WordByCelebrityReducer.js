import {wordByCelebrity} from "../library/game3/round_5";

const initialState = {
	wordByCelebrity: wordByCelebrity
};

const wordByCelebrityReducer = (state = initialState, action) => {
	switch (action.type) {

		default: return state
	}
};

export default wordByCelebrityReducer;